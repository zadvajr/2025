-- Select Data
-- To retrieve data from a database, we make use of SELECT statement

-- Specify Columns
-- by specifying column name we can choose which column to select
-- Example
SELECT brand, model FROM cars; -- will select only brand and model column from cars table

-- Return All Columns
-- To select all columns from a table, we make use of * instead of column names
--Example
SELECT * FROM cars;